flask
flask-cors
requests

# API Framework
fastapi
uvicorn[standard]
pydantic

# LLM Integration (Modern SDKs)
google-genai>=0.2.0
openai>=1.0.0
python-dotenv
httpx

# Optional: Local LLM via Ollama
# ollama-python

# -------------------------------------------------------
# PATHWAY — Real-Time Streaming Engine
# -------------------------------------------------------
# Pathway does NOT install on Windows natively.
# The setup.bat handles this automatically via WSL Ubuntu.
#
# If you are on Linux/macOS, install manually:
#   pip install pathway
#
# If you are on Windows (without WSL), the app runs in
# DEMO mode using built-in mock data — no install needed.
# -------------------------------------------------------
# pathway  <-- intentionally commented out for Windows safety
